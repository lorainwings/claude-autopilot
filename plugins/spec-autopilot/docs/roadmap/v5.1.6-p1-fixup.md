# Role: Senior Performance Engineer & DX Architect (资深性能工程师 & 开发者体验架构师)

# Objective: 执行 spec-autopilot v5.1.6 高 ROI 效能与体验冲刺

在最新的工业级仿真评测中，系统斩获了 88.5 分。为了稳步突破 90 分大关，我们需要对评测中暴露的最高 ROI（投资回报率）盲点进行“外科手术式”的定点清除。本次任务的原则是：**代码改动极小，效能与体验收益极大，绝对稳定。**

## 🛑 绝对红线：稳定性与测试纪律

1. **测试不可侵犯**：我们现有的 55 个测试文件是核心资产。你的任何修改**必须保证** `tests/run_all.sh` 依然 100% 通过。
2. **严禁篡改编排**：绝对不允许修改 `SKILL.md` 等 L3 层核心编排逻辑。本次仅聚焦 L2 提速、构建清理与 GUI 优化。
3. **补充边界测试**：针对你修改的脚本逻辑（如跳过特定文件后缀、Enum 枚举校验），必须在现有对应的测试文件中新增断言用例。

---

## 🛠️ 高 ROI 修复任务矩阵 (High-ROI Remediation Matrix)

请分模块执行以下任务。建议每完成一个模块，运行测试确认无误后，再推进下一模块。

### 模块一：L2 拦截器极速瘦身 (I/O Performance)

* **任务 1：跳过非源码文件的重度校验 (对应盲点 #5)**
  * **行动**：修改 `scripts/unified-write-edit-check.sh`。在执行 CHECK 2 (TODO), CHECK 3 (恒真断言), CHECK 4 (代码约束) 之前，增加文件扩展名白名单检测。如果是 `.md`, `.json`, `.yaml`, `.yml`, `.txt`, `.csv` 等非源码格式，**直接跳过这 3 项检查**（但必须保留 CHECK 0 状态隔离和 CHECK 1 TDD 隔离）。以此大幅斩断 Python 启动与正则匹配的时间开销。

### 模块二：GUI 容错防呆与渲染降压 (Frontend UX & FPS)

* **任务 2：决策面板的 WebSocket 断线感知 (对应盲点 #12)**
  * **行动**：修改 `gui/src/components/GateBlockCard.tsx`。通过读取当前 WebSocket 的连接状态 (`connected`)，当网络断开时，**强行禁用** Retry/Fix/Override 三个按钮，并在卡片显著位置渲染红色警告语：“网络已断开，请等待重连后操作”，彻底消除静默失败。
* **任务 3：高频 Selector 的 Memoization (对应盲点 #11)**
  * **行动**：修改 `gui/src/store/index.ts` 或对应的消费组件（如 `PhaseTimeline`, `TelemetryDashboard`）。利用 `useMemo` 或 Zustand 的 `useShallow` 对 `selectPhaseDurations`、`selectTotalElapsedMs` 等高频纯函数进行严格缓存。确保 1 秒定时器触发时，若底层 `events` 未实质改变，不会引发深层 React 重渲染。

### 模块三：DX 工程成熟度完善 (DevOps & Config)

* **任务 4：配置校验增加 Enum 强校验 (对应盲点 #15)**
  * **行动**：修改 `scripts/_config_validator.py`。新增 `ENUM_RULES` 字典（例如限制 `phases.reporting.format` 只能为 `['allure', 'custom']`）。在验证主循环中增加逻辑，若字段值不在枚举列表内，直接抛出配置错误，实现 DX 错误拦截前置。
* **任务 5：构建产物隔离 Node 依赖 (对应盲点 #16)**
  * **行动**：修改 `scripts/build-dist.sh`。在打包过滤逻辑中，明确排除 `scripts/node_modules/` 目录，防止服务端本地开发依赖泄露到最终的 `dist/` 产物中。

---

## 🚀 交付路径

1. 先处理 **模块一** 和 **模块三**（Bash 和 Python），因为修改后可以立刻通过 `bash tests/run_all.sh` 验证你的代码没有破坏现有的底层防御。
2. 再处理 **模块二**（React 前端），确保执行 `npm run build` 无语法错误。

如果你已清楚这五个“低风险、高收益”的靶向任务，请回复：“高 ROI 效能冲刺引擎已启动，正在执行 L2 提速、GUI 防呆与产物净化！”并开始严格输出代码。
