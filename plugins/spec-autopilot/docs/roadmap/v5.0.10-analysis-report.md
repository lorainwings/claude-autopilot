# Role: Master Orchestrator & Chief AI Engineering Auditor (首席工程大脑 & 终极评估中枢)

# Objective: 全面接管并进行全自动复测 `spec-autopilot` (v5.3 GUI V2 满血版)。系统近期完成了底层的 Token 瘦身、细粒度进度遥测、测试模块化，以及最核心的 GUI V2 赛博朋克风三栏架构重构。你需要启动 7 个并行 Sub-Agent 全自动执行测试，在 v5.1.1 的基准上进行全量提分评估与 Bug 扫雷，并输出最新的总控仪表盘

## ⚠️ 全局执行指令 (Global Execution Rules)

1. **全自动闭环 (Fully Autonomous):** 你具备完整的执行权限。请自行在后台拉起 `gui/` 的构建与运行进程、执行 `tests/run_all.sh` 模块化测试。
2. **靶向 V2 新特性 (V2 Focus):** 严查 `gui-dist/` 产物是否纯净、WebSocket `decision_ack` 竞态问题是否彻底解决、ANSI 终端着色是否精准。
3. **输出规范:** 每个任务生成独立的 `-v5.3.md` 报告，统一输出到 `docs/reports/` 目录下。完成所有任务后，必须生成全局总控仪表盘。

---

## 🛠️ 分发任务清单 (Task Delegation)

### [Agent 1] 任务一：底层测试基建与构建产物审查

- **执行动作:** 运行 `tests/run_all.sh`，检查 49 个独立测试模块的通过率。执行 `build-dist.sh`。
- **复测靶点:** 1. `dist/spec-autopilot/` 产物是否成功剔除了 `gui/` 源码和测试资产，体积是否达标。2. `CLAUDE.md` 中的测试铁律防线是否坚固。
- **输出:** `docs/reports/infrastructure-audit-v5.3.md`

### [Agent 2] 任务二：需求路由与 Socratic 引擎评估

- **审计重点:** 验证 Phase 1 中新增的 `requirement_type` 复合路由逻辑，以及 Step 7 对“非功能需求（SLA/性能）”的质询能力。测试 `min_qa_rounds` 强制消费逻辑。
- **输出:** `docs/reports/routing-socratic-benchmark-v5.3.md`

### [Agent 3] 任务三：实时遥测与性能压测 (Performance & Telemetry)

- **审计重点:** 评估新引入的 `emit-task-progress.sh` 细粒度事件流对主线程的性能损耗。验证 Phase 5 中按需加载并行协议（`parallel-phase*.md`）带来的 Token 瘦身率（预期 >60%）。
- **输出:** `docs/reports/performance-benchmark-v5.3.md`

### [Agent 4] 任务四：Vibe Coding 顶级竞品综合对比 (重点 UI 跃升)

- **审计重点:** 基于全新的 GUI V2（JetBrains Mono 字体、三栏布局、SVG 环形图），重新对标 Cursor / Windsurf 的内置终端界面。评估其“沉浸式黑客体验”的护城河深度。
- **输出:** `docs/reports/competitive-analysis-v5.3.md`

### [Agent 5] 任务五：全局规约与 TDD 隔离度审查

- **审计重点:** 验证 TDD-5 回滚机制（REFACTOR 失败时全文件 `git checkout -- .` 回滚）的确定性。验证崩溃恢复清理 `rm -f .tdd-stage` 是否生效。
- **输出:** `docs/reports/compliance-audit-v5.3.md`

### [Agent 6] 任务六：GUI V2 交互与状态机边界极限测试 (CRITICAL)

- **复测靶点 (猛攻 13 项修复):** 1. 暴力触发 `GateBlock` 并发送决策，验证 **G2 (ack 竞态)** 和 **G1 (幽灵复现)** 是否彻底解决。
  2. 检查 `PhaseTimeline` 在 Block 被解除后，是否能正确恢复 Running (**G4**)。
  3. 观察 `VirtualTerminal` 是否成功解析了后端的红绿黄 ANSI 转义码，并且追加了 payload 详情 (**G7**)。
  4. 故意抛出前端异常，测试 **G11 (Error Boundary)** 降级 UI 是否生效。
  5. 验证 `fix_instructions` 文本框的内容能否准确无误地投递给引擎。
- **输出:** `docs/reports/gui-interaction-audit-v5.3.md`

### [Agent 7] 任务七：全链路整体性真实业务仿真测试

- **执行动作:** 拉起一个真实的业务 Demo，跑通从 Phase 0 到 Phase 7 的完整流转。
- **审计重点:** 观察左侧 Timeline、中央看板、下方终端、右侧监控大盘（四大组件）在高速并发下的数据同步率和视觉流畅度（Framer Motion 动画是否卡顿）。
- **输出:** `docs/reports/holistic-simulation-benchmark-v5.3.md`

### [Master] 终极任务：生成 v5.3 满血版总控仪表盘

- **动作:** 汇总 Agent 1-7 的最新评分。
- **计算:** 对比 `v5.1.1` 的 84.4 分基线，量化 GUI V2 重构和模块化测试带来的提升数值（Delta），给出冲击 95+ 分的最终评估结果与遗留优化建议。
- **输出:** 必须生成最终大盘：`docs/reports/v5.3-evaluation-dashboard.md`。

---

## 🚀 启动口令

请确认你已清楚本次针对 v5.3 GUI V2 架构的深度审计目标。如果准备就绪，请回复“v5.3 满血版全自动评估引擎已点火”，并立即拉起 Sub-Agents 从 Task 1 开始执行毁灭性测试！
