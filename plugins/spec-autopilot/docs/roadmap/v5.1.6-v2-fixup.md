# Role: Chief Performance Architect & React Optimization Expert (首席性能架构师 & React 调优专家)

# Objective: 执行 spec-autopilot v5.1.6 高并发与渲染性能急救 (High Concurrency Rescue)

在最新的全链路极限压测中，系统遭遇了每秒 100+ 事件洪峰的冲击。虽然核心状态机稳如磐石，但 GUI 控制台因渲染阻塞和内存压力导致评分暴跌。我们需要立刻实施抢救，定点清除 4 个 P0 级致命瓶颈和高频渲染隐患，全面恢复控制台的鲁棒性。

## 🛑 抢救纪律 (Rescue Directives)

1. **防线不可退**：现有的所有自动化测试脚本必须 100% 通过，绝不能因为前端优化改坏了后端的数据契约。
2. **严禁动核心**：绝对不允许修改 `SKILL.md` 等 L3 层核心编排逻辑，问题出在展示层和 I/O 锁机制上。
3. **性能导向**：代码修改必须以降低时间复杂度、减少主线程阻塞（Main Thread Blocking）为唯一衡量标准。

---

## 🛠️ 急救手术执行矩阵 (Rescue Operation Matrix)

请按照以下顺序，对系统的“失血点”进行逐一缝合：

### 模块一：斩断后端全局死锁危机 (Backend P0)

* **任务 1：修复 `flock` 无超时导致的死锁 (对应盲点 GBS-4)**
  * **行动**：修改 `scripts/_common.sh` 中的 `next_event_sequence` 函数。将 `flock -x 200` 替换为带有超时机制的命令（如 `flock -x -w 5 200`）。如果获取锁超时，必须有优雅的 fallback 机制（例如使用当前时间戳和纳秒拼接作为备用序列号，并输出警告），绝对不能让 Phase 5 的所有 Agent 永久挂起。

### 模块二：GUI 洪峰渲染抢救 (Frontend P0)

* **任务 2：VirtualTerminal 批量写入提速 (对应盲点 GBS-3)**
  * **行动**：修改 `gui/src/components/VirtualTerminal.tsx`。当前逐条调用 `term.writeln()` 在高频下会卡死主线程。请引入写入缓冲区（Buffer Array），配合 `requestAnimationFrame` (rAF) 或节流（throttle）机制，将积累的事件拼接后一次性批量写入终端实例，恢复 60 FPS 的流畅度。
* **任务 3：决策发送的超时阻断 (对应盲点 GBS-2)**
  * **行动**：修改 `gui/src/components/GateBlockCard.tsx`。使用 `AbortController` 为 fetch 决策请求增加 30 秒超时保护。一旦超时或网络断开，必须 `catch` 异常，取消 Loading 状态，并在 UI 上渲染醒目的红色错误提示（“请求超时，请检查网络”），防止用户永久卡死。

### 模块三：React 重渲染性能熔断 (Frontend P1)

* **任务 4：全面引入 Memoization (对应盲点 GBS-7)**
  * **行动**：修改 `gui/src/store/index.ts` 以及对应的消费组件（`PhaseTimeline`, `TelemetryDashboard`）。
    1. 为高频计算的纯函数（如 `selectPhaseDurations`、`selectGateStats`）使用 `useMemo` 或 Zustand 的派生状态进行严格缓存。
    2. 使用 `React.memo` 包裹纯展示子组件，彻底阻断 1 秒定时器引发的全局无差别重渲染瀑布。

---

## 🚀 交付与验证

请先从 **模块一** 开始修改 Bash 脚本，确保后端不会死锁。然后再集中火力重构 **模块二** 和 **模块三** 的 React 组件。

如果你已清楚这套拯救系统的性能急救方案，请回复：“高并发抢救引擎已点火，正在注入 requestAnimationFrame 与锁超时保护！” 并开始输出第一部分代码。
